<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.his.mapper.HisEhealthClientMapper">
    
    <resultMap type="HisEhealthClient" id="HisEhealthClientResult">
        <result property="id"    column="id"    />
        <result property="appId"    column="app_id"    />
        <result property="appSecret"    column="app_secret"    />
        <result property="orgCode"    column="org_code"    />
        <result property="orgName"    column="org_name"    />
        <result property="openApi"    column="open_api"    />
        <result property="version"    column="version"    />
        <result property="createTime"    column="create_time"    />
        <result property="deleted"    column="deleted"    />
    </resultMap>

    <sql id="selectHisEhealthClientVo">
        select id, app_id, app_secret, org_code, org_name, open_api, version, create_time, deleted from his_ehealth_client
    </sql>

    <select id="selectHisEhealthClientList" parameterType="HisEhealthClient" resultMap="HisEhealthClientResult">
        <include refid="selectHisEhealthClientVo"/>
        <where>  
            <if test="appId != null  and appId != ''"> and app_id = #{appId}</if>
            <if test="orgName != null  and orgName != ''"> and org_name like concat('%', #{orgName}, '%')</if>
        </where>
    </select>
    
    <select id="selectHisEhealthClientById" parameterType="Long" resultMap="HisEhealthClientResult">
        <include refid="selectHisEhealthClientVo"/>
        where id = #{id}
    </select>

</mapper>