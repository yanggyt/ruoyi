<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.busi.mapper.BusiProductRequireMapper">
    
    <resultMap type="BusiProductRequire" id="BusiProductRequireResult">
        <result property="id"    column="id"    />
        <result property="orderId"    column="order_id"    />
        <result property="orderName"    column="order_name"    />
        <result property="amount"    column="amount"    />
        <result property="size"    column="size"    />
        <result property="color"    column="color"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="endDate"    column="end_date"    />
    </resultMap>

    <resultMap id="BusiProductRequireBusiMaterialRequireResult" type="BusiProductRequire" extends="BusiProductRequireResult">
        <collection property="busiMaterialRequireList" notNullColumn="sub_id" javaType="java.util.List" resultMap="BusiMaterialRequireResult" />
    </resultMap>

    <resultMap type="BusiMaterialRequire" id="BusiMaterialRequireResult">
        <result property="id"    column="sub_id"    />
        <result property="productRequireId"    column="sub_product_require_id"    />
        <result property="amount"    column="sub_amount"    />
        <result property="unit"    column="sub_unit"    />
        <result property="color"    column="sub_color"    />
        <result property="classify"    column="sub_classify"    />
        <result property="createBy"    column="sub_create_by"    />
        <result property="createTime"    column="sub_create_time"    />
        <result property="updateBy"    column="sub_update_by"    />
        <result property="updateTime"    column="sub_update_time"    />
    </resultMap>

    <sql id="selectBusiProductRequireVo">
        select A.id, A.order_id,B.order_name, A.amount,A.size, A.color, A.create_by, A.create_time, A.update_by, A.update_time, A.end_date
        from busi_product_require A LEFT JOIN busi_order B on A.order_id = B.id
    </sql>

    <select id="selectBusiProductRequireList" parameterType="BusiProductRequire" resultMap="BusiProductRequireResult">
        <include refid="selectBusiProductRequireVo"/>
        <where>  
            <if test="orderId != null  and orderId != ''"> and A.order_id = #{orderId}</if>
            <if test="size != null  and size != ''"> and A.size = #{size}</if>
            <if test="color != null  and color != ''"> and A.color = #{color}</if>
        </where>
        order by A.update_time desc
    </select>
    
    <select id="selectBusiProductRequireById" parameterType="String" resultMap="BusiProductRequireBusiMaterialRequireResult">
        select a.id, a.order_id,c.order_name, a.amount, a.size, a.color, a.create_by, a.create_time, a.update_by, a.update_time, a.end_date,
            b.id as sub_id, b.product_require_id as sub_product_require_id, b.amount as sub_amount, b.unit as sub_unit, b.color as sub_color, b.classify as sub_classify, b.create_by as sub_create_by, b.create_time as sub_create_time, b.update_by as sub_update_by, b.update_time as sub_update_time
        from busi_product_require a
        left join busi_material_require b on b.product_require_id = a.id
        left join busi_order c on c.id = a.order_id
        where a.id = #{id}
    </select>
        
    <insert id="insertBusiProductRequire" parameterType="BusiProductRequire" useGeneratedKeys="true" keyProperty="id">
        insert into busi_product_require
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="orderId != null and orderId != ''">order_id,</if>
            <if test="amount != null">amount,</if>
            <if test="size != null and size != ''">size,</if>
            <if test="color != null and color != ''">color,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="endDate != null">end_date,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="orderId != null and orderId != ''">#{orderId},</if>
            <if test="amount != null">#{amount},</if>
            <if test="size != null and size != ''">#{size},</if>
            <if test="color != null and color != ''">#{color},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="endDate != null">#{endDate},</if>
         </trim>
    </insert>

    <update id="updateBusiProductRequire" parameterType="BusiProductRequire">
        update busi_product_require
        <trim prefix="SET" suffixOverrides=",">
            <if test="orderId != null and orderId != ''">order_id = #{orderId},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="size != null and size != ''">size = #{size},</if>
            <if test="color != null and color != ''">color = #{color},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteBusiProductRequireById" parameterType="String">
        delete from busi_product_require where id = #{id}
    </delete>

    <delete id="deleteBusiProductRequireByIds" parameterType="String">
        delete from busi_product_require where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
    <delete id="deleteBusiMaterialRequireByProductRequireIds" parameterType="String">
        delete from busi_material_require where product_require_id in 
        <foreach item="productRequireId" collection="array" open="(" separator="," close=")">
            #{productRequireId}
        </foreach>
    </delete>

    <delete id="deleteBusiMaterialRequireByProductRequireId" parameterType="String">
        delete from busi_material_require where product_require_id = #{productRequireId}
    </delete>

    <insert id="batchBusiMaterialRequire">
        insert into busi_material_require( id, product_require_id, amount, unit, color, classify, create_by, create_time, update_by, update_time) values
		<foreach item="item" index="index" collection="list" separator=",">
            ( #{item.id}, #{item.productRequireId}, #{item.amount}, #{item.unit}, #{item.color}, #{item.classify}, #{item.createBy}, #{item.createTime}, #{item.updateBy}, #{item.updateTime})
        </foreach>
    </insert>


    <select id="selectMaterialRequireByOrderId" parameterType="String" resultType="java.util.Map">
        select max(A.classify) classifyCode,max(sdd2.dict_label) classifyVal, max(A.color) colorCode, max(sdd.dict_label) colorVal,sum(A.amount) amount, MAX(sdd3.dict_label) unitVal, max(A.unit) as unitCode
          from busi_material_require A
               left join busi_product_require B on A.product_require_id = B.id
               left join busi_order bo on B.order_id = bo.id
               left join sys_dict_data sdd on sdd.dict_type='busi_color' and A.color = sdd.dict_value
               left join sys_dict_data sdd2 on sdd2.dict_type='busi_material_type' and A.classify = sdd2.dict_value
               left join sys_dict_data sdd3 on sdd3.dict_type='busi_material_unit' and A.unit = sdd3.dict_value
        where B.order_id = #{orderId} group by (A.color + A.classify)
    </select>
</mapper>